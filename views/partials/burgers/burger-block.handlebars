
 <h1>{{id}}</h1>
 
    <br> {{#if complete}}
    <button class="change-status" data-id="{{id}}" data-newstatus="{{complete}}">
        EAT THIS
    </button>
    {{else}}
    <button class="delete-burger btn-primary" data-id="{{id}}">DELETE!</button>
    {{/if}} {{!--
    <button class="delete-burger btn-primary" data-id="{{this.id}}">DELETE!</button> --}}
